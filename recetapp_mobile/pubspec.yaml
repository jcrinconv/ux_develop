name: recetapp_mobile
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_screenutil: ^5.9.3
  gap: ^3.0.1
  camera: ^0.12.1
  get_it: ^9.2.1
  google_fonts: ^6.2.1

dev_dependencies:
  build_runner: ^2.4.8
  flutter_lints: ^6.0.0
  build_resolvers: ^2.4.2
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.6.0
  retrofit_generator: ^8.0.0

flutter:
  uses-material-design: true
