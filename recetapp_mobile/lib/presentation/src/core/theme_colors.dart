import 'package:flutter/material.dart';

abstract class RecetappThemeColors {
  // Verdes
  static const Color mint = Color(0xFF9FE8D6);
  static const Color primary = Color(0xFF0E9A82);
  static const Color darkGreen = Color(0xFF0C2622);

  // Coral / naranja
  static const Color peach = Color(0xFFFFB8A3);
  static const Color accent = Color(0xFFFF6F4D);

  // Fondo
  static const Color background = Color(0xFFF5FBF9);

  // Alias semánticos
  static const Color error = accent;
  static const Color onPrimary = Colors.white;
  static const Color textPrimary = darkGreen;
}
